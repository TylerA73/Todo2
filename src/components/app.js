import React, { Component } from 'react';
import AddBar from '../containers/add_bar.js';

export default class App extends Component {
  render() {
    return (
      <div>
      	<AddBar/>
      </div>
    );
  }
}
